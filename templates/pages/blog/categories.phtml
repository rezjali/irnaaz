<div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <div class="md:col-span-1">
        <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="text-lg font-semibold mb-4">افزودن دسته جدید</h3>
            <form action="<?php echo APP_URL; ?>/blog/categories/store" method="POST">
                <div class="space-y-4">
                    <div>
                        <label for="name" class="block text-sm font-medium">نام دسته</label>
                        <input type="text" name="name" id="name" required class="w-full px-4 py-2 border rounded-lg mt-1">
                    </div>
                    <div><button type="submit" class="w-full bg-blue-600 text-white font-bold py-2 px-4 rounded-lg">افزودن</button></div>
                </div>
            </form>
        </div>
    </div>
    <div class="md:col-span-2">
        <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="text-lg font-semibold mb-4">لیست دسته‌ها</h3>
            <div class="overflow-x-auto">
                <table class="w-full text-sm text-right text-gray-500">
                    <thead class="text-xs uppercase bg-gray-50">
                        <tr><th class="py-3 px-6">نام</th><th class="py-3 px-6">اسلاگ</th><th class="py-3 px-6">عملیات</th></tr>
                    </thead>
                    <tbody>
                        <?php foreach ($categories as $category): ?>
                        <tr class="bg-white border-b">
                            <td class="py-4 px-6 font-medium"><?php echo e($category->name); ?></td>
                            <td class="py-4 px-6"><?php echo e($category->slug); ?></td>
                            <td class="py-4 px-6"><a href="#" class="font-medium text-red-600 hover:underline">حذف</a></td>
                        </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
