<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <!-- Add New Rate Form -->
    <div class="lg:col-span-1">
        <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="text-lg font-semibold mb-4">افزودن نرخ جدید</h3>
            <form action="<?php echo APP_URL; ?>/constants/shipping-rates/store" method="POST">
                <div class="space-y-4">
                    <div>
                        <label for="description" class="block text-sm font-medium text-gray-700 mb-1">توضیحات نرخ</label>
                        <input type="text" name="description" id="description" required
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                               placeholder="مثال: بسته تا ۱ کیلوگرم">
                    </div>
                    <div>
                        <label for="cost" class="block text-sm font-medium text-gray-700 mb-1">هزینه (تومان)</label>
                        <input type="number" step="any" name="cost" id="cost" required
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                               placeholder="مثال: 150000">
                    </div>
                    <div>
                        <button type="submit" class="w-full bg-blue-600 text-white font-bold py-2 px-4 rounded-lg hover:bg-blue-700">
                            افزودن
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Rates Table -->
    <div class="lg:col-span-2">
        <div class="bg-white p-6 rounded-xl shadow-md">
             <h3 class="text-lg font-semibold mb-4">لیست نرخ‌های موجود</h3>
            <div class="overflow-x-auto">
                <table class="w-full text-sm text-right text-gray-500">
                    <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                        <tr>
                            <th scope="col" class="py-3 px-6">#</th>
                            <th scope="col" class="py-3 px-6">توضیحات</th>
                            <th scope="col" class="py-3 px-6">هزینه</th>
                            <th scope="col" class="py-3 px-6">عملیات</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($rates as $rate): ?>
                            <tr class="bg-white border-b hover:bg-gray-50">
                                <td class="py-4 px-6"><?php echo e($rate->id); ?></td>
                                <td class="py-4 px-6 font-medium text-gray-900"><?php echo e($rate->description); ?></td>
                                <td class="py-4 px-6"><?php echo number_format($rate->cost); ?> تومان</td>
                                <td class="py-4 px-6">
                                    <form action="<?php echo APP_URL; ?>/constants/shipping-rates/delete" method="POST" onsubmit="return confirm('آیا از حذف این نرخ مطمئن هستید؟');">
                                        <input type="hidden" name="id" value="<?php echo e($rate->id); ?>">
                                        <button type="submit" class="font-medium text-red-600 hover:underline">حذف</button>
                                    </form>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
