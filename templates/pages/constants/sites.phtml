<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <!-- Add New Site Form -->
    <div class="lg:col-span-1">
        <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="text-lg font-semibold mb-4">افزودن سایت جدید</h3>
            <form action="<?php echo APP_URL; ?>/constants/sites/store" method="POST">
                <div class="space-y-4">
                    <div>
                        <label for="site_name" class="block text-sm font-medium text-gray-700 mb-1">نام سایت</label>
                        <input type="text" name="site_name" id="site_name" required
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                               placeholder="مثال: Trendyol">
                    </div>
                    <div>
                        <label for="site_url" class="block text-sm font-medium text-gray-700 mb-1">آدرس سایت (URL)</label>
                        <input type="url" name="site_url" id="site_url" required dir="ltr"
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                               placeholder="https://www.example.com">
                    </div>
                    <div>
                        <button type="submit" class="w-full bg-blue-600 text-white font-bold py-2 px-4 rounded-lg hover:bg-blue-700">
                            افزودن
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Sites Table -->
    <div class="lg:col-span-2">
        <div class="bg-white p-6 rounded-xl shadow-md">
             <h3 class="text-lg font-semibold mb-4">لیست سایت‌های مرجع</h3>
            <div class="overflow-x-auto">
                <table class="w-full text-sm text-right text-gray-500">
                    <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                        <tr>
                            <th scope="col" class="py-3 px-6">#</th>
                            <th scope="col" class="py-3 px-6">نام سایت</th>
                            <th scope="col" class="py-3 px-6">آدرس</th>
                            <th scope="col" class="py-3 px-6">عملیات</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($sites as $site): ?>
                            <tr class="bg-white border-b hover:bg-gray-50">
                                <td class="py-4 px-6"><?php echo e($site->id); ?></td>
                                <td class="py-4 px-6 font-medium text-gray-900"><?php echo e($site->site_name); ?></td>
                                <td class="py-4 px-6" dir="ltr"><a href="<?php echo e($site->site_url); ?>" target="_blank" class="text-blue-600 hover:underline"><?php echo e($site->site_url); ?></a></td>
                                <td class="py-4 px-6">
                                    <form action="<?php echo APP_URL; ?>/constants/sites/delete" method="POST" onsubmit="return confirm('آیا از حذف این سایت مطمئن هستید؟');">
                                        <input type="hidden" name="id" value="<?php echo e($site->id); ?>">
                                        <button type="submit" class="font-medium text-red-600 hover:underline">حذف</button>
                                    </form>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
