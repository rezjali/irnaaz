<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <div class="lg:col-span-1">
        <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="text-lg font-semibold mb-4">افزودن آیتم جدید</h3>
            <form action="<?php echo APP_URL; ?>/menus/store" method="POST">
                <input type="hidden" name="group_id" value="<?php echo e($current_group_id); ?>">
                <div class="space-y-4">
                    <div>
                        <label for="title" class="block text-sm font-medium">عنوان</label>
                        <input type="text" name="title" id="title" required class="w-full px-4 py-2 border rounded-lg mt-1">
                    </div>
                    <div>
                        <label for="url" class="block text-sm font-medium">لینک (URL)</label>
                        <input type="text" name="url" id="url" required class="w-full px-4 py-2 border rounded-lg mt-1" dir="ltr" placeholder="/about-us">
                    </div>
                    <div><button type="submit" class="w-full bg-blue-600 text-white font-bold py-2 px-4 rounded-lg">افزودن</button></div>
                </div>
            </form>
        </div>
    </div>
    <div class="lg:col-span-2">
        <div class="bg-white p-6 rounded-xl shadow-md">
            <div class="flex items-center mb-4">
                 <h3 class="text-lg font-semibold">آیتم‌های منو:</h3>
                 <form id="group_switcher" class="mr-4">
                     <select name="group_id" class="px-3 py-2 border rounded-lg" onchange="document.getElementById('group_switcher').submit()">
                         <?php foreach ($groups as $group): ?>
                         <option value="<?php echo e($group->id); ?>" <?php echo $current_group_id == $group->id ? 'selected' : ''; ?>><?php echo e($group->name); ?></option>
                         <?php endforeach; ?>
                     </select>
                 </form>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full text-sm text-right text-gray-500">
                    <thead class="text-xs uppercase bg-gray-50">
                        <tr><th class="py-3 px-6">عنوان</th><th class="py-3 px-6">لینک</th><th class="py-3 px-6">عملیات</th></tr>
                    </thead>
                    <tbody>
                        <?php foreach ($menu_items as $item): ?>
                        <tr class="bg-white border-b">
                            <td class="py-4 px-6 font-medium"><?php echo e($item->title); ?></td>
                            <td class="py-4 px-6" dir="ltr"><?php echo e($item->url); ?></td>
                            <td class="py-4 px-6"><a href="#" class="font-medium text-red-600 hover:underline">حذف</a></td>
                        </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
