<div class="bg-white p-6 rounded-xl shadow-md">
    <div class="flex justify-between items-center mb-4">
        <h2 class="text-xl font-semibold"><?php echo e($title); ?></h2>
        <a href="<?php echo APP_URL; ?>/orders/create" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700">ثبت سفارش جدید</a>
    </div>

    <!-- Search and Filter Form -->
    <form method="GET" action="" class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
        <div class="md:col-span-2">
            <input type="text" name="search" placeholder="جستجو..."
                   class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                   value="<?php echo e($_GET['search'] ?? ''); ?>">
        </div>
        <div>
            <select name="status_id" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" onchange="this.form.submit()">
                <option value="">
                    <?php 
                        if ($current_category === 'active') echo 'همه سفارشات فعال';
                        elseif ($current_category === 'cancelled') echo 'همه کنسل شده ها';
                        elseif ($current_category === 'deleted') echo 'همه حذف شده ها';
                        else echo 'همه';
                    ?>
                </option>
                <?php foreach ($statuses as $status): ?>
                    <option value="<?php echo e($status->id); ?>" <?php echo (isset($_GET['status_id']) && $_GET['status_id'] == $status->id) ? 'selected' : ''; ?>>
                        <?php echo e($status->status_name); ?>
                    </option>
                <?php endforeach; ?>
            </select>
        </div>
        <div>
            <button type="submit" class="w-full bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700">فیلتر</button>
        </div>
    </form>

    <!-- Orders Table -->
    <div class="overflow-x-auto">
        <table class="w-full text-sm text-right text-gray-500">
            <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                <tr>
                    <th scope="col" class="py-3 px-6">کد سفارش</th>
                    <th scope="col" class="py-3 px-6">مشتری</th>
                    <th scope="col" class="py-3 px-6">مبلغ کل</th>
                    <th scope="col" class="py-3 px-6">وضعیت</th>
                    <th scope="col" class="py-3 px-6">تاریخ ثبت</th>
                    <th scope="col" class="py-3 px-6">عملیات</th>
                </tr>
            </thead>
            <tbody>
                <?php if (empty($orders)): ?>
                    <tr><td colspan="6" class="text-center py-6">هیچ سفارشی یافت نشد.</td></tr>
                <?php else: ?>
                    <?php foreach ($orders as $order): ?>
                        <tr class="bg-white border-b hover:bg-gray-50">
                            <td class="py-4 px-6 font-medium">#<?php echo e($order->id); ?></td>
                            <td class="py-4 px-6"><?php echo e($order->customer_name); ?></td>
                            <td class="py-4 px-6"><?php echo number_format($order->total_cost); ?> تومان</td>
                            <td class="py-4 px-6"><span class="bg-blue-100 text-blue-800 text-xs font-medium px-2.5 py-0.5 rounded-full"><?php echo e($order->status_name); ?></span></td>
                            <td class="py-4 px-6"><?php echo jdate('Y/m/d H:i', strtotime($order->created_at)); ?></td>
                            <td class="py-4 px-6"><a href="#" class="font-medium text-blue-600 hover:underline">مشاهده</a></td>
                        </tr>
                    <?php endforeach; ?>
                <?php endif; ?>
            </tbody>
        </table>
    </div>

    <!-- Pagination -->
    <div class="mt-6">
        <?php require_once TEMPLATES_PATH . '/partials/_pagination.php'; ?>
    </div>
</div>
