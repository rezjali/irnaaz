<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <!-- Add New Role Form -->
    <div class="lg:col-span-1">
        <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="text-lg font-semibold mb-4">ایجاد گروه جدید</h3>
            <form action="<?php echo APP_URL; ?>/roles/store" method="POST">
                <div class="space-y-4">
                    <div>
                        <label for="role_name" class="block text-sm font-medium text-gray-700 mb-1">نام گروه</label>
                        <input type="text" name="role_name" id="role_name" required
                               class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                               placeholder="مثال: مسئول خرید">
                    </div>
                    <div>
                        <button type="submit" class="w-full bg-blue-600 text-white font-bold py-2 px-4 rounded-lg hover:bg-blue-700">
                            ایجاد
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Roles Table -->
    <div class="lg:col-span-2">
        <div class="bg-white p-6 rounded-xl shadow-md">
             <h3 class="text-lg font-semibold mb-4">لیست گروه‌های کاربری</h3>
             <?php display_flash_message('success'); ?>
             <?php display_flash_message('error'); ?>
            <div class="overflow-x-auto">
                <table class="w-full text-sm text-right text-gray-500">
                    <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                        <tr>
                            <th scope="col" class="py-3 px-6">#</th>
                            <th scope="col" class="py-3 px-6">نام گروه</th>
                            <th scope="col" class="py-3 px-6">تعداد کاربران</th>
                            <th scope="col" class="py-3 px-6">عملیات</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php if (empty($roles)): ?>
                            <tr>
                                <td colspan="4" class="text-center py-6">هیچ گروهی یافت نشد.</td>
                            </tr>
                        <?php else: ?>
                            <?php foreach ($roles as $role): ?>
                                <tr class="bg-white border-b hover:bg-gray-50">
                                    <td class="py-4 px-6"><?php echo e($role->id); ?></td>
                                    <td class="py-4 px-6 font-medium text-gray-900"><?php echo e($role->role_name); ?></td>
                                    <td class="py-4 px-6"><?php echo e($role->user_count); ?> نفر</td>
                                    <td class="py-4 px-6 flex items-center space-x-4 space-x-reverse">
                                        <?php if ($role->role_key !== 'super_admin'): ?>
                                            <a href="<?php echo APP_URL; ?>/roles/edit?id=<?php echo e($role->id); ?>" class="font-medium text-blue-600 hover:underline">ویرایش دسترسی‌ها</a>
                                            <?php if ($role->user_count == 0): ?>
                                                <form action="<?php echo APP_URL; ?>/roles/delete" method="POST" onsubmit="return confirm('آیا از حذف این گروه مطمئن هستید؟');">
                                                    <input type="hidden" name="id" value="<?php echo e($role->id); ?>">
                                                    <button type="submit" class="font-medium text-red-600 hover:underline">حذف</button>
                                                </form>
                                            <?php endif; ?>
                                        <?php else: ?>
                                            <span class="text-gray-400">غیرقابل ویرایش</span>
                                        <?php endif; ?>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        <?php endif; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
