<div class="bg-white p-6 rounded-xl shadow-md max-w-2xl mx-auto">
    <h2 class="text-xl font-semibold mb-6 border-b pb-4">تنظیمات ماژول سفارش‌ها</h2>

    <form action="<?php echo APP_URL; ?>/settings/update" method="POST">
        <div class="space-y-6">
            <!-- Allow New Orders -->
            <div>
                <label class="block text-sm font-medium text-gray-700">امکان ثبت سفارش جدید</label>
                <div class="mt-2 flex items-center space-x-4 space-x-reverse">
                    <label class="flex items-center">
                        <input type="radio" name="order_allow_new" value="1" class="h-4 w-4" <?php echo ($settings['order_allow_new'] ?? '1') == '1' ? 'checked' : ''; ?>>
                        <span class="mr-2">فعال</span>
                    </label>
                    <label class="flex items-center">
                        <input type="radio" name="order_allow_new" value="0" class="h-4 w-4" <?php echo ($settings['order_allow_new'] ?? '1') == '0' ? 'checked' : ''; ?>>
                        <span class="mr-2">غیرفعال</span>
                    </label>
                </div>
            </div>

            <!-- Default Status -->
            <div>
                <label for="order_default_status_on_create" class="block text-sm font-medium text-gray-700 mb-1">وضعیت پیش‌فرض هنگام ثبت سفارش</label>
                <select name="order_default_status_on_create" id="order_default_status_on_create" class="w-full px-4 py-2 border rounded-lg">
                    <?php foreach ($statuses as $status): ?>
                        <option value="<?php echo e($status->id); ?>" <?php echo ($settings['order_default_status_on_create'] ?? 1) == $status->id ? 'selected' : ''; ?>>
                            <?php echo e($status->status_name); ?>
                        </option>
                    <?php endforeach; ?>
                </select>
            </div>

            <!-- SMS Notification -->
            <div>
                <label class="block text-sm font-medium text-gray-700">ارسال پیامک هنگام تغییر وضعیت</label>
                <div class="mt-2 flex items-center space-x-4 space-x-reverse">
                    <label class="flex items-center">
                        <input type="radio" name="order_send_sms_on_status_change" value="1" class="h-4 w-4" <?php echo ($settings['order_send_sms_on_status_change'] ?? '0') == '1' ? 'checked' : ''; ?>>
                        <span class="mr-2">فعال</span>
                    </label>
                    <label class="flex items-center">
                        <input type="radio" name="order_send_sms_on_status_change" value="0" class="h-4 w-4" <?php echo ($settings['order_send_sms_on_status_change'] ?? '0') == '0' ? 'checked' : ''; ?>>
                        <span class="mr-2">غیرفعال</span>
                    </label>
                </div>
            </div>
        </div>

        <!-- Form Actions -->
        <div class="mt-8 pt-5 border-t">
            <div class="flex justify-end">
                <button type="submit" class="bg-blue-600 text-white font-bold py-2 px-6 rounded-lg hover:bg-blue-700">ذخیره تغییرات</button>
            </div>
        </div>
    </form>
</div>
