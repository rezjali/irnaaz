<div class="bg-white p-6 rounded-xl shadow-md max-w-2xl mx-auto">
    <h2 class="text-xl font-semibold mb-6 border-b pb-4">تنظیمات پنل پیامک</h2>

    <form action="<?php echo APP_URL; ?>/settings/update" method="POST">
        <div class="space-y-6">
            <!-- SMS Enabled -->
            <div>
                <label class="block text-sm font-medium text-gray-700">وضعیت کلی ارسال پیامک</label>
                <div class="mt-2 flex items-center space-x-4 space-x-reverse">
                    <label class="flex items-center">
                        <input type="radio" name="sms_enabled" value="1" class="h-4 w-4" <?php echo ($settings['sms_enabled'] ?? '0') == '1' ? 'checked' : ''; ?>>
                        <span class="mr-2">فعال</span>
                    </label>
                    <label class="flex items-center">
                        <input type="radio" name="sms_enabled" value="0" class="h-4 w-4" <?php echo ($settings['sms_enabled'] ?? '0') == '0' ? 'checked' : ''; ?>>
                        <span class="mr-2">غیرفعال</span>
                    </label>
                </div>
            </div>

            <!-- API Key -->
            <div>
                <label for="sms_api_key" class="block text-sm font-medium text-gray-700 mb-1">کلید API پنل پیامک</label>
                <input type="text" name="sms_api_key" id="sms_api_key" dir="ltr"
                       class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                       value="<?php echo e($settings['sms_api_key'] ?? ''); ?>">
            </div>

            <!-- Sender Number -->
            <div>
                <label for="sms_sender_number" class="block text-sm font-medium text-gray-700 mb-1">شماره خط ارسال کننده</label>
                <input type="text" name="sms_sender_number" id="sms_sender_number" dir="ltr"
                       class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                       value="<?php echo e($settings['sms_sender_number'] ?? ''); ?>">
            </div>
            
            <!-- Template for Status Change -->
            <div>
                <label for="sms_template_status_change" class="block text-sm font-medium text-gray-700 mb-1">قالب پیامک تغییر وضعیت سفارش</label>
                <textarea name="sms_template_status_change" id="sms_template_status_change" rows="4"
                          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"><?php echo e($settings['sms_template_status_change'] ?? ''); ?></textarea>
                <p class="text-xs text-gray-500 mt-1">
                    می‌توانید از متغیر <code>{status}</code> برای نمایش نام وضعیت جدید استفاده کنید.
                </p>
            </div>
        </div>

        <!-- Form Actions -->
        <div class="mt-8 pt-5 border-t">
            <div class="flex justify-end">
                <button type="submit" class="bg-blue-600 text-white font-bold py-2 px-6 rounded-lg hover:bg-blue-700">ذخیره تغییرات</button>
            </div>
        </div>
    </form>
</div>
