<div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <!-- Assign Order Form -->
    <div class="lg:col-span-1">
        <div class="bg-white p-6 rounded-xl shadow-md">
            <h3 class="text-lg font-semibold mb-4">تخصیص سفارش به چمدان</h3>
            <form action="<?php echo APP_URL; ?>/suitcases/assign-order" method="POST">
                <input type="hidden" name="suitcase_id" value="<?php echo e($suitcase->id); ?>">
                <div class="space-y-4">
                    <div>
                        <label for="order_id" class="block text-sm font-medium text-gray-700 mb-1">انتخاب سفارش</label>
                        <select name="order_id" id="order_id" required class="w-full px-4 py-2 border border-gray-300 rounded-lg">
                            <option value="">یک سفارش را انتخاب کنید</option>
                            <?php foreach ($unassigned_orders as $order): ?>
                                <option value="<?php echo e($order->id); ?>">سفارش #<?php echo e($order->id); ?> - <?php echo e($order->customer_name); ?></option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                    <div>
                        <button type="submit" class="w-full bg-blue-600 text-white font-bold py-2 px-4 rounded-lg hover:bg-blue-700">افزودن به چمدان</button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Assigned Orders Table -->
    <div class="lg:col-span-2">
        <div class="bg-white p-6 rounded-xl shadow-md">
             <h3 class="text-lg font-semibold mb-4">سفارشات داخل چمدان #<?php echo e($suitcase->suitcase_code); ?></h3>
            <div class="overflow-x-auto">
                <table class="w-full text-sm text-right text-gray-500">
                    <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                        <tr>
                            <th scope="col" class="py-3 px-6">کد سفارش</th>
                            <th scope="col" class="py-3 px-6">مشتری</th>
                            <th scope="col" class="py-3 px-6">عملیات</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php if (empty($assigned_orders)): ?>
                            <tr><td colspan="3" class="text-center py-6">هیچ سفارشی به این چمدان تخصیص داده نشده است.</td></tr>
                        <?php else: ?>
                            <?php foreach ($assigned_orders as $order): ?>
                                <tr class="bg-white border-b">
                                    <td class="py-4 px-6 font-medium">#<?php echo e($order->id); ?></td>
                                    <td class="py-4 px-6"><?php echo e($order->customer_name); ?></td>
                                    <td class="py-4 px-6">
                                        <form action="<?php echo APP_URL; ?>/suitcases/remove-order" method="POST">
                                            <input type="hidden" name="suitcase_id" value="<?php echo e($suitcase->id); ?>">
                                            <input type="hidden" name="order_id" value="<?php echo e($order->id); ?>">
                                            <button type="submit" class="font-medium text-red-600 hover:underline">حذف</button>
                                        </form>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        <?php endif; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
