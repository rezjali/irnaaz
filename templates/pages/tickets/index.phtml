<div class="bg-white p-6 rounded-xl shadow-md">
    <div class="flex justify-between items-center mb-4">
        <h2 class="text-xl font-semibold">لیست تیکت‌ها</h2>
        <a href="#" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700">ایجاد تیکت جدید</a>
    </div>

    <!-- Tickets Table -->
    <div class="overflow-x-auto">
        <table class="w-full text-sm text-right text-gray-500">
            <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                <tr>
                    <th scope="col" class="py-3 px-6">شناسه</th>
                    <th scope="col" class="py-3 px-6">موضوع</th>
                    <th scope="col" class="py-3 px-6">ارسال کننده</th>
                    <th scope="col" class="py-3 px-6">وضعیت</th>
                    <th scope="col" class="py-3 px-6">آخرین بروزرسانی</th>
                    <th scope="col" class="py-3 px-6">عملیات</th>
                </tr>
            </thead>
            <tbody>
                <?php if (empty($tickets)): ?>
                    <tr>
                        <td colspan="6" class="text-center py-6">هیچ تیکتی یافت نشد.</td>
                    </tr>
                <?php else: ?>
                    <?php foreach ($tickets as $ticket): ?>
                        <tr class="bg-white border-b hover:bg-gray-50">
                            <td class="py-4 px-6 font-medium text-gray-900">#<?php echo e($ticket->id); ?></td>
                            <td class="py-4 px-6"><?php echo e($ticket->subject); ?></td>
                            <td class="py-4 px-6"><?php echo e($ticket->user_name); ?></td>
                            <td class="py-4 px-6">
                                <?php 
                                    $statusClass = 'bg-gray-100 text-gray-800';
                                    if ($ticket->status === 'open') $statusClass = 'bg-green-100 text-green-800';
                                    if ($ticket->status === 'answered') $statusClass = 'bg-blue-100 text-blue-800';
                                    if ($ticket->status === 'closed') $statusClass = 'bg-red-100 text-red-800';
                                    
                                    $statusText = ['open' => 'باز', 'answered' => 'پاسخ داده شده', 'closed' => 'بسته شده'];
                                ?>
                                <span class="text-xs font-medium px-2.5 py-0.5 rounded-full <?php echo $statusClass; ?>">
                                    <?php echo $statusText[$ticket->status] ?? e($ticket->status); ?>
                                </span>
                            </td>
                            <td class="py-4 px-6"><?php echo jdate('Y/m/d H:i', strtotime($ticket->updated_at)); ?></td>
                            <td class="py-4 px-6">
                                <a href="<?php echo APP_URL; ?>/tickets/show?id=<?php echo e($ticket->id); ?>" class="font-medium text-blue-600 hover:underline">مشاهده و پاسخ</a>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                <?php endif; ?>
            </tbody>
        </table>
    </div>

    <!-- Pagination -->
    <div class="mt-6">
        <?php require_once TEMPLATES_PATH . '/partials/_pagination.php'; ?>
    </div>
</div>
